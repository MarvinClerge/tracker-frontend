const initState = {

}

export deafault function rootReducer(state = initState, action) => {
  switch (action.type) {
    case "ACTION":
      // return the state obj
    default:
      return state;
  }
}
